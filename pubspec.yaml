name: scheduled_timer
description: A simple timer that can be scheduled to run at a specific time, and will restore it's scheduled time across app restarts.
version: 2.0.1
homepage: https://github.com/magnuswikhog/scheduled_timer

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.0.11



dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
